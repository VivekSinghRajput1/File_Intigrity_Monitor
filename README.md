A Python-based File Integrity Monitoring (FIM) tool that scans directories and generates SHA-256 hash baselines of files.
It can later compare current file states against the baseline to detect modifications, deletions, and newly added files. 
This tool helps ensure system integrity and detect unauthorized changes in files.
